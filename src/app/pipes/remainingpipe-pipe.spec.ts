import { RemainingPipe } from './remainingpipe-pipe';

describe('RemainingPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingPipe();
    expect(pipe).toBeTruthy();
  });
});
