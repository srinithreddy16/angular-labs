import { DisableCopyPaste } from './disable-copy-paste';

describe('DisableCopyPaste', () => {
  it('should create an instance', () => {
    const directive = new DisableCopyPaste();
    expect(directive).toBeTruthy();
  });
});
